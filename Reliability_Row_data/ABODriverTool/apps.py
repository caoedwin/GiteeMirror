from django.apps import AppConfig


class AbodrivertoolConfig(AppConfig):
    #  不能为名称，应该是app的路径
    name = 'ABODriverTool'
    # 中文要显示的名称
    # verbose_name = "博客"
    # verbose_name_plural = verbose_name
